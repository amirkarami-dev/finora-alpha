/// <reference types="vite/client" />

// Fontsource packages are CSS-only side-effect imports; declare them so `tsc` resolves them.
declare module '@fontsource-variable/inter';
declare module '@fontsource-variable/vazirmatn';
