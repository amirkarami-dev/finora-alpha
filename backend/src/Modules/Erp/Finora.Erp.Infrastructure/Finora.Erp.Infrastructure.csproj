<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Finora.Erp.Application\Finora.Erp.Application.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\Finora.BuildingBlocks.Infrastructure\Finora.BuildingBlocks.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" />
  </ItemGroup>

  <ItemGroup>
    <!-- The money arithmetic is internal because nothing outside this module should call it, but
         its rules are exactly the ones worth testing in isolation, with a hand-built list and no
         database in the way. -->
    <InternalsVisibleTo Include="Finora.UnitTests" />
  </ItemGroup>

</Project>
